# .streamlit/secrets.toml
# Not required for local use.
# If deploying to Streamlit Cloud, you can set DATA_DIR here
# if your CSVs are hosted somewhere accessible (e.g. a shared drive or URL).
# For local CSV files, use the sidebar path input in the dashboard instead.

# DATA_DIR = "/path/to/your/csvs"
